<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.torikatsu923.refma">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_refma_icon"
            android:label="@string/app_name"
            android:roundIcon="@drawable/ic_refma_icon"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".TutorialActivity">
        </activity>
        <activity android:name=".EditDataActivity">
        </activity>
        <activity android:name=".ChooseIconActivity">
        </activity>
        <activity android:name=".AddDataActivity">
        </activity>
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>